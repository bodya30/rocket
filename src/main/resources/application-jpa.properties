spring.datasource.url=jdbc:mysql://localhost:3306/rocket2?autoReconnect=true&useSSL=false
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

#Used to execute data.sql, because by default it executes only with embedded db
spring.datasource.initialization-mode=always